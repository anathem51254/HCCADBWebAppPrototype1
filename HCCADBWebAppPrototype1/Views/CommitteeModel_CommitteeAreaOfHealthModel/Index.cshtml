@model IEnumerable<HCCADBWebAppPrototype1.Models.CommitteeModel_CommitteeAreaOfHealthModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CommitteeAreaOfHealthModel.AreaOfHealthName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommitteeModel.CommitteeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CommitteeAreaOfHealthModel.AreaOfHealthName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CommitteeModel.CommitteeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommitteeModel_CommitteeAreaOfHealthModelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommitteeModel_CommitteeAreaOfHealthModelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommitteeModel_CommitteeAreaOfHealthModelID })
        </td>
    </tr>
}

</table>
