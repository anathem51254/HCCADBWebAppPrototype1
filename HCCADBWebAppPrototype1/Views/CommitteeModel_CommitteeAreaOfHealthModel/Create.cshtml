@model HCCADBWebAppPrototype1.Models.CommitteeModel_CommitteeAreaOfHealthModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CommitteeModel_CommitteeAreaOfHealthModel</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.CommitteeModelID, "CommitteeModelID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("CommitteeModelID", String.Empty)
                @Html.ValidationMessageFor(model => model.CommitteeModelID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.CommitteeAreaOfHealthModelID, "CommitteeAreaOfHealthModelID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("CommitteeAreaOfHealthModelID", String.Empty)
                @Html.ValidationMessageFor(model => model.CommitteeAreaOfHealthModelID)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
